# 维护必读

这份文档解决一个问题：**这个项目在本机有三份拷贝，改了一处忘了同步就会出事。**

---

## 一、三份拷贝是什么关系

```
   ┌─────────────────────────────────────────────────┐
   │  gh-publish  ← 唯一源，所有改动都在这里做          │
   │  D:\X Files\DSH\dsh01\gh-publish                │
   └─────────────────────────────────────────────────┘
          │
          ├── git push ──────→ GitHub 仓库（别人 clone 的）
          │
          ├── build_dist ────→ dist\*.zip（发 Release 用）
          │
          └── setup.py ──────→ 已安装那份（你自己机器上实际跑的）
                               %USERPROFILE%\.cursor\skills\vedio-concentrator
```

**记住一句话：只改 `gh-publish`，其他两份都是它的产物。**

| 拷贝 | 作用 | 谁能改 |
|---|---|---|
| **gh-publish** | 唯一源 + Git 仓库 | ✅ 只有它 |
| 已安装那份 | Agent 实际执行的 | ❌ 由 `setup.py` 自动覆盖 |
| `vedio-concentrator` | 旧的打包源（已退休） | ❌ 别动，或直接删掉 |

> `vedio-concentrator` 这个文件夹是历史遗留。它当初的职责（打包）现在已经由
> `gh-publish\scripts\build_dist.py` 承担，所以它只是备份，留着以防万一。

---

## 二、日常：改完代码怎么发布

### 双击一下就行

打开仓库文件夹，双击 **`更新.cmd`**，它自动做四件事：

```
[1/4] 检查三份拷贝有没有分叉
[2/4] 打包 dist\*.zip
[3/4] 同步到你本机装的那份
[4/4] 提交并推送到 GitHub
```

### 不想推送到 GitHub

```powershell
cd D:\X Files\DSH\dsh01\gh-publish
powershell -ExecutionPolicy Bypass -File scripts\build_bundle.ps1 -NoPush
```

其他参数：

| 参数 | 作用 |
|---|---|
| `-NoPush` | 只打包 + 同步本机，不碰 git |
| `-NoCommit` | 列出改动但不自动提交（你想自己在 Desktop 里提交） |
| `-Message "说明"` | 自定义提交说明，默认是"更新 2026-09-25 14:30" |

---

## 三、要发新版给别人用

`更新.cmd` 只把代码推到 GitHub，**zip 附件还得你手动传到 Release**——这是设计如此，因为 Release 需要写版本说明。

1. 改 `scripts/build_dist.py` 里的版本号：

   ```python
   VERSION = "1.0.1"
   ```

2. 双击 `更新.cmd`

3. 打开 https://github.com/Edoardo-Zhang/vedio-concentrator/releases/new

4. 填 tag（`v1.0.1`）、标题、说明，**附件拖入**：

   ```
   D:\X Files\DSH\dsh01\gh-publish\dist\vedio-concentrator-bundle.zip
   ```

5. Publish release

### 版本号怎么定

| 改了什么 | 版本号 | 例子 |
|---|---|---|
| 修 bug | `1.0.0` → `1.0.1` | 修代理探测 |
| 加功能 | `1.0.0` → `1.1.0` | 加抖音支持 |
| 大改 / 不兼容 | `1.0.0` → `2.0.0` | 换 ASR 引擎 |

⚠️ **旧 Release 永远不要删**——有人可能正在用旧版，删了他的下载链接就断了。

---

## 四、护栏：check_sync.py 是干什么的

它防止你"改了一处忘了另一处"：

```powershell
cd D:\X Files\DSH\dsh01\gh-publish
python scripts\check_sync.py
```

输出示例：

```
=== 三份拷贝同步检查 ===
  [有] Git仓库    17 个文件  D:\X Files\DSH\dsh01\gh-publish
  [有] 打包源     16 个文件  D:\X Files\DSH\dsh01\vedio-concentrator
  [有] 已安装     10 个文件  C:\Users\Lenovo\.cursor\skills\vedio-concentrator

!! 检测到分叉:
   有的拷贝缺这个文件：
      scripts/check_sync.py          缺失: 打包源
```

它比对内容时会**忽略 BOM、换行、编码差异**，所以不会误报（`.cmd` 是 GBK、
`.ps1` 有 BOM，这些格式差异不算分叉）。

有分叉时返回码是 1，`更新.cmd` 会警告但不拦你——因为有时你就是想先打包看看。

### 两条比对规则

| 关系 | 规则 |
|---|---|
| Git仓库 ↔ 打包源 | **镜像**，一个文件都不能差 |
| Git仓库 → 已安装 | **子集**，只有该跑的那些文件需要一致 |

`check_sync.py` 里有两个名单，**新加了文件记得同步加进去**：

```python
PAIR_FILES  = [...]   # 仓库里应当存在的全部文件
RUNTIME_SET = {...}   # 需要装到「已安装」那份的文件
```

---

## 五、常见故障

| 现象 | 原因 / 处理 |
|---|---|
| `更新.cmd` 报"检测到分叉" | 按提示把缺的文件从 `gh-publish` 复制过去 |
| 双击 `更新.cmd` 一闪而过 | 在 cmd 里执行它，就能看到完整报错 |
| `git push` 卡住 | `git config --global http.proxy http://127.0.0.1:7897`，推完用 `--unset http.proxy` 取消 |
| 推送报 `Authentication failed` | 凭据过期。控制面板 → 凭据管理器 → Windows 凭据 → 删掉 `git:https://github.com`，重推会再弹登录 |
| 你本机跑的还是旧版 | 跑 `python scripts\setup.py --skill-only` 同步 |
| 发了 Release 但别人说装的是旧代码 | 打包源没同步，`check_sync.py` 能查出来 |

---

## 六、绝对不要提交的东西

`.gitignore` 已经挡住大部分，但你自己也要注意：

| 不要提交 | 为什么 |
|---|---|
| `*.mp4` `*.wav` `*.srt` | 下载的视频和转写稿，几百 MB 且涉版权 |
| `models/` | 1.5 GB，GitHub 单文件超 100 MB 直接拒收 |
| `dist/` | 可选。想让人直接拿到 zip 就提交，否则靠 Release |
| `*.log` `__pycache__/` | 垃圾 |

检查：

```powershell
git status
git ls-files        # 已经在仓库里的文件
```

---

## 七、一条命令速查

```powershell
cd D:\X Files\DSH\dsh01\gh-publish

# 日常更新（等价于双击 更新.cmd）
powershell -ExecutionPolicy Bypass -File scripts\build_bundle.ps1

# 只打包，不推 GitHub
python scripts\build_dist.py

# 同步到你本机装的那份
python scripts\setup.py --skill-only

# 查分叉
python scripts\check_sync.py

# 环境自检（含 GPU）
python scripts\setup_check.py
```
